#ifndef YK_RENDERER_H
#define YK_RENDERER_H

/*
    ToDo(facts): Front end / back end divisions. Also stick to a consistent naming scheme.
                 I haven't done it yet since I'm stil learning vulkan.
*/

#include <renderer/mn_types.h>
#include <renderer/descriptors.h>
#include <renderer/renderer.h>
#include <renderer/yk_texture.h>

#endif
